/" Análise de PCR em tempo real "/
function calcular_cn(amostra: string, valor_ct: float): string {
    if (valor_ct <= 20.0) {
        return "Alta_Concentracao";
    } else if (valor_ct > 35.0) {
        return "Nao_Detectado";
    }
    amostra = amostra + " Analisada"; // Concatenação suportada
    var bool resultado = true;
    return "Resultado";
}

var x = 0.001;
x += 0.001;
